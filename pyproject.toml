[build-system]
requires = ["setuptools>=42.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--cov=pelandobot"
testpaths = [
    "tests",
]

[tool.black]
line-length = 105
--extend-exclude = "variables_and_parameters.py"